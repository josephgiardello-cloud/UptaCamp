[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[packages]
pygame = ">=2.4.0"
websockets = ">=12.0"
python-dotenv = ">=1.0.1"
sentry-sdk = ">=2.29.1"

[dev-packages]
pytest = ">=7.4.0"
pytest-cov = ">=4.1.0"
black = ">=23.7.0"
ruff = ">=0.0.287"
mypy = ">=1.5.0"

[requires]
python_version = "3.10"
